# Copy this file to ~/.config/codex-keeper/config.toml and edit from there.
# This starter config only controls local session manager paths and UI language.

[general]
# Usually you can keep the default Codex home.
codex_home = "~/.codex"

# sitJac/codex-keeper keeps its own SQLite state and runtime files here.
state_dir = "~/.local/state/codex-keeper"

# UI language for Web and CLI messages.
ui_language = "zh-CN"
